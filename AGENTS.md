This project's personal-context pointer, conventions, and recovery workflow are maintained in
[.github/copilot-instructions.md](.github/copilot-instructions.md) — read it before working in
this repo. That file is the single source of truth; this pointer exists only so Codex (which
does not read `.github/copilot-instructions.md`) picks up the same instructions.

